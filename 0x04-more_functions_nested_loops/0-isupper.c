#include "main.h"

/**
 * _isupper  - checks for uppercase character
 * @c: Variable text
 * Return: Always 0
 */

in _isupper(int c)
{
	if(c >='A' && C <= 'z')

	{
		return(1);
	}
	else
	{
		return(0);
	}

}
		
